describe("Store User Actions", () => {
  describe("loginUser", () => {
    it("should login and set user state", () => {
      // const typeTests = {
      //   setUserAuth: payload => {
      //     expect(payload).toBeEqual(user);
      //   },
      //   setUserStories: payload => {}
      // };
      // const commit = (type, payload) => {
      //   typeTests[type](payload);
      // };
      // const user = {
      //   uid: 123
      // };
      // loginUser({ commit }, user);
    });
  });
});
